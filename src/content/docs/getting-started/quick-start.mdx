---
title: Quick Start
lastUpdated: 2026-05-28
description: Clone fullstackhero and run it locally in under a minute. (The fsh CLI is coming soon.)
sidebar:
  order: 3
pageType: guide
seo:
  title: 'Quick Start — clone and run the .NET 10 starter kit'
  description: 'Clone the fullstackhero repo and run it locally via .NET Aspire. The FullStackHero.CLI dotnet tool is coming soon for one-command scaffolding.'
  keywords: 'fullstackhero quick start, clone .NET 10 starter kit, .NET Aspire local development, fsh CLI'
---

<Callout type="warning" title="CLI coming soon">
The one-command `fsh new` flow needs the `FullStackHero.CLI` NuGet tool, which
isn't published yet — it lands soon. Until then, the fastest working path is a
**git clone**, shown below. See [Install](/docs/getting-started/install/) for
every path.
</Callout>

The quickest way to run the kit today is to clone the repo and start it with .NET Aspire.

## 1. Clone the repo

```bash
git clone https://github.com/fullstackhero/dotnet-starter-kit.git MyApp
cd MyApp
```

A clone keeps the original `FSH.Starter` project names — it doesn't rename the solution. Once the CLI ships, `fsh new MyApp` will scaffold a fully renamed project (and `fsh doctor` will verify your environment first).

## 2. Run

```bash
dotnet run --project src/Host/FSH.Starter.AppHost
```

Aspire brings up Postgres and Valkey containers, wires connection strings, launches the API host, and enables OTLP export. Open the Aspire dashboard to watch services start; the API serves at `https://localhost:5285` with OpenAPI and Scalar at `/scalar/v1`.

## Next

- **[Install](/docs/getting-started/install/)** — every install path (GitHub template, Codespaces, and the upcoming CLI + `dotnet new` template).
- **[Architecture](/docs/architecture/)** — the modular monolith + VSA mental model.
- **[CLI](/docs/cli/)** — every `fsh` command and option (coming soon).
